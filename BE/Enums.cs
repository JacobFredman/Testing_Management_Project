namespace BE
{
    public enum Gender { Male, Female }
    /// <summary>
    /// 
    /// </summary>
    public enum LicenceType { B, A2, A1, A, C1, C, D, D1, D2, D3, E, _1 }
    public enum Gear { Automat, Manual }
    public enum Days { Sunday, Monday, Tuersday, Wednesday, Thursday, Friday, Saturday }
}
